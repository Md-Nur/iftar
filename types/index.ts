export interface IftarLocation {
    id: string
    name: string
    area?: string
    iftar_type: string
    target_audience: string
    lat: number
    lng: number
    date?: string
    created_at: string
}
